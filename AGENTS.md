# For any agent

Your brief for this workspace is [CLAUDE.md](CLAUDE.md) — read it and operate by it.

(This pointer exists because some agent harnesses auto-load `AGENTS.md` rather than `CLAUDE.md`. Same brief either way; it lives in one file.)
