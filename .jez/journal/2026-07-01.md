---
date: 2026-07-01
---

# 2026-07-01

## What I worked on
Mined `~/Documents/removals-fleet` for generalisable agent-working patterns worth pulling into the dotjez toolkit. Jez had been learning a lot there (prompting, "wiki", kanban) and wanted the reusable ideas surfaced.

## What I found
The fleet is largely a staging ground for dotjez — `agentic-work-understanding.md` literally says "promote to a global skill once grounded", and its `fleet-knowledge-base.md` already cites our `derived-files-generated-not-hand-kept` rule. So most of its 27 transferable patterns are already covered here. Sorted into: genuinely-new, sharper-articulation, already-covered (full menu given to Jez in-chat).

The one clear capability gap was the **kanban-as-folders** pattern (card = file, folder = status, `git mv` to claim, worktree-safe concurrency). The "wiki" Jez mentioned turned out to be the fleet's three-level condensing memory + a `[[wikilink]]` fact substrate (`data/reference/`), not a literal wiki — same shape as our "where patterns live".

## What landed
- New skill `skills/kanban/` — `SKILL.md` (the folder-board pattern, generalised, removals specifics stripped) + `board.mjs`, a zero-dependency Node web UI to view and drag cards (uses `git mv` in a repo, plain rename otherwise; polls disk for live updates). Tested end-to-end: board discovery, column ordering, frontmatter parsing, git-mv move (staged as rename), duplicate-name + path-traversal guards, and a visual check of the rendered board.
- Added kanban to `skills/README.md`. Plugin auto-discovers from the folder, so no manifest edit needed.

## What I noticed
Jez confirmed the "complex prompt → worse outcome" concern is already well covered in `write-prompts` (four angles). The one thing the fleet adds that *isn't* explicit: over-specifying *creative/divergent* work doesn't just bloat, it **homogenises** outputs. Offered it as a one-sentence add; he didn't take the broader enhancement bundle this round.

## What's next
If Jez wants them later: the "dial as master principle" + multi-agent failure modes (blind-agents-converge, enumerate-N) into `write-prompts`/`orchestrate-agents`; "guards over vigilance" as a rule; understanding-capture into `reflect`; verify-the-verifier; "the verb steers the material" into `write-image-prompts`; KB facts-not-prose volatility tags into `truth-seeking`. The full ranked menu is in this session.
